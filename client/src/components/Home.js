import React from 'react';

export default function Home() {
    return (
        <div>
            To play Kakuro, the goal is to fill each row with digits between 1 - 9 (each cell containing a unique entry). Along with that, each row/column of digits should sum up to a corresponding value to the left/top of the set.
        </div>
    )
};
